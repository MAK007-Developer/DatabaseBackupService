# Database Backup Service
This a windows service that backups a specified database every time interval the user determine in .config file
also logs every event while it's running and handles errors gracefully through errorr handling best practices.
